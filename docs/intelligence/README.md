# Frontend Placement
Runtime UI belongs in the existing:
- updated-frontend/app/ → routes/pages
- updated-frontend/components/ → reusable UI
- updated-frontend/lib/ → API clients, types, utilities
Reuse existing auth/RBAC/design system.
